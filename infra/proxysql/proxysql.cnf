datadir="/var/lib/proxysql"

admin_variables=
{
    admin_credentials="admin:admin123"
    mysql_ifaces="0.0.0.0:6032"
}

mysql_variables=
{
    threads=4
    max_connections=2048
    default_query_delay=0
    default_query_timeout=36000000
    have_compress=true
    poll_timeout=2000
    interfaces="0.0.0.0:6033"
    default_schema="fileshare_db"
    stacksize=1048576
    server_version="8.0.35"
    connect_timeout_server=3000
    monitor_username="monitor"
    monitor_password="monitor123"
    monitor_history=600000
    monitor_connect_interval=60000
    monitor_ping_interval=10000
    monitor_read_only_interval=1500
    monitor_read_only_timeout=500
    ping_interval_server_msec=120000
    ping_timeout_server=500
    commands_stats=true
    sessions_sort=true
    connect_retries_on_failure=10
    # Force mysql_native_password for MySQL 8.0 compatibility
    mysql_default_authentication_plugin="mysql_native_password"
}

mysql_servers =
(
    {
        address = "mysql-writer"
        port = 3306
        hostgroup = 0
        max_connections = 100
        weight = 1000
        comment = "MySQL Writer"
    },
    {
        address = "mysql-replica1"
        port = 3306
        hostgroup = 1
        max_connections = 100
        weight = 500
        comment = "MySQL Replica 1"
    },
    {
        address = "mysql-replica2"
        port = 3306
        hostgroup = 1
        max_connections = 100
        weight = 500
        comment = "MySQL Replica 2"
    }
)

mysql_users =
(
    {
        username = "fileshare_user"
        password = "FileShare@123!"
        default_hostgroup = 0
        max_connections = 1000
        default_schema = "fileshare_db"
        active = 1
    },
    {
        username = "root"
        password = "RootP@ssw0rd!2024"
        default_hostgroup = 0
        max_connections = 100
        default_schema = "fileshare_db"
        active = 1
    }
)

mysql_query_rules =
(
    {
        rule_id = 1
        active = 1
        match_pattern = "^SELECT .* FOR UPDATE$"
        destination_hostgroup = 0
        apply = 1
    },
    {
        rule_id = 2
        active = 1
        match_pattern = "^SELECT"
        destination_hostgroup = 1
        apply = 1
    },
    {
        rule_id = 3
        active = 1
        match_pattern = ".*"
        destination_hostgroup = 0
        apply = 1
    }
)
